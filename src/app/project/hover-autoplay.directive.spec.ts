import { HoverAutoplayDirective } from './hover-autoplay.directive';

describe('HoverAutoplayDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverAutoplayDirective();
    expect(directive).toBeTruthy();
  });
});
